import React from 'react';
import { Text, View } from 'react-native';

function Header() {
    return(
<Text style={{ fontWeight:'bold' ,textAlign:'center', backgroundColor:'orange', flexDirection:'row', paddingTop:40 }}>Some Text</Text>    );
  }

export default Header;